language: cpp

os: osx
osx_image: xcode12

before_script:
  - sh ./openFrameworks/scripts/osx/download_libs.sh
  - mkdir build
  - cd build
  - cmake ..

script:
  - make
  - ./app -i ../instances/sample.txt -s CAUSAL_PIBT_MAPF -v
  - cd ../visualizer
  - make build
